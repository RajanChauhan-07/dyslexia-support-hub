import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dyslexiasupporthub.app',
  appName: 'Dyslexia Support Hub',
  webDir: 'dist'
};

export default config;
